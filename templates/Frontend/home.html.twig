{% extends 'base.html.twig' %}

{% block title %}
        Mes couilles
{% endblock %}

{% block body %}

     <p>{{ titre|raw }}</p>

        <a href="{{ path('app_francis_homepage') }}">Home</a>
        <a href="{{ path('app_showone', { ma_wildcard: 'francis', autre_truc: 'huster' }) }}">Show</a>


<h1>Il y a : {{ reponses|length }} réponses</h1>

<ul>
    {% for reponse in reponses %}
        <li>{{ reponse }}</li>
    {% endfor %}
</ul>
{% endblock %}


{% block stylesheets %}
        {{ parent() }}
        <link rel="stylesheet" href="/css/{{ asset('css/style.css') }}">
{% endblock %}
